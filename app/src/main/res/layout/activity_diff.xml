<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        app:titleTextAppearance="@style/CompactToolbarTitle"
        app:subtitleTextAppearance="@style/CompactToolbarSubtitle"
        android:background="@color/toolbar"
        app:titleTextColor="@color/white"
        app:subtitleTextColor="#B3FFFFFF"
        app:navigationIcon="@drawable/ic_up" />

    <FrameLayout
        android:id="@+id/overlay_host"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Direction is set in code: side-by-side / stacked (see DiffActivity.applyLayoutMode) -->
        <LinearLayout
            android:id="@+id/panes"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:baselineAligned="false">

            <!-- Title strip at the top of each column, indicating which side is old/new
                 (or left/right) — without it, in side-by-side mode you have to guess from
                 position, and in portrait (one side at a time) there is no clue at all. -->
            <LinearLayout
                android:id="@+id/side_left"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/title_left"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="@color/path_bar_active"
                    android:paddingStart="8dp"
                    android:paddingEnd="8dp"
                    android:paddingTop="3dp"
                    android:paddingBottom="3dp"
                    android:singleLine="true"
                    android:ellipsize="start"
                    android:textColor="@color/white"
                    android:textSize="11sp" />

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/list_left"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent" />
            </LinearLayout>

            <View
                android:id="@+id/divider"
                android:layout_width="1dp"
                android:layout_height="match_parent"
                android:background="@color/divider" />

            <LinearLayout
                android:id="@+id/side_right"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/title_right"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="@color/path_bar_active"
                    android:paddingStart="8dp"
                    android:paddingEnd="8dp"
                    android:paddingTop="3dp"
                    android:paddingBottom="3dp"
                    android:singleLine="true"
                    android:ellipsize="start"
                    android:textColor="@color/white"
                    android:textSize="11sp" />

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/list_right"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent" />
            </LinearLayout>
        </LinearLayout>

        <TextView
            android:id="@+id/tv_empty"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:textColor="@color/text_secondary"
            android:visibility="gone" />

        <ProgressBar
            android:id="@+id/loading"
            style="?android:attr/progressBarStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center" />

        <!-- Floating button for merging a diff hunk: position is computed in code (see
             DiffActivity.updateMergePill) — it tracks the currently-focused hunk, sticks
             to the divider line in side-by-side mode and follows scroll; when invisible
             the whole pill goes GONE. x/y are placed directly via View.setX/setY rather
             than layout_gravity, because the position varies continuously and changing
             margins is more awkward than translations. -->
        <LinearLayout
            android:id="@+id/merge_pill"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:background="@drawable/bg_merge_pill"
            android:elevation="4dp"
            android:visibility="gone">

            <ImageButton
                android:id="@+id/btn_merge_a"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:padding="4dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:scaleType="fitCenter" />

            <ImageButton
                android:id="@+id/btn_merge_b"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:padding="4dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:scaleType="fitCenter" />
        </LinearLayout>
    </FrameLayout>
</LinearLayout>
