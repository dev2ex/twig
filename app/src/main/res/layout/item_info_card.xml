<?xml version="1.0" encoding="utf-8"?>
<!-- 属性卡片:tab 标题栏是随树缩进的小条(与文件行图标对齐,像树节点),内容盒左右填满 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="4dp"
    android:paddingEnd="4dp"
    android:paddingTop="2dp"
    android:paddingBottom="2dp">

    <LinearLayout
        android:id="@+id/tab_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="-1dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/info_tab_bg" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/info_card_bg">

        <!-- 目录递归统计的转圈叠在内容盒右下角(就在它描述的那两行数字旁边)。
             ★ 不能放进 tab 条那一行:tab 条是 wrap_content + 随树深度增长的 marginStart,
             双面板下半屏宽的面板里层级一深就把整行吃满,固定宽的转圈被排到右边界外裁掉,
             肉眼看就是"没有转圈"。叠在内容盒里挤不出去。
             ★ 也不能在绑定时动态 addView:tab 条每次绑定都 removeAllViews,
             转圈会随扫描进度的每次刷新被重建、动画一直从头开始。 -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/info_body"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingStart="10dp"
                android:paddingEnd="10dp"
                android:paddingTop="8dp"
                android:paddingBottom="8dp"
                android:lineSpacingExtra="2dp"
                android:textColor="@color/text_primary"
                android:textSize="12sp" />

            <ProgressBar
                android:id="@+id/scan_spin"
                style="?android:attr/progressBarStyleSmall"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:layout_gravity="bottom|end"
                android:layout_marginEnd="8dp"
                android:layout_marginBottom="8dp"
                android:visibility="gone" />
        </FrameLayout>

        <TextView
            android:id="@+id/btn_hash"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="8dp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:paddingTop="5dp"
            android:paddingBottom="5dp"
            android:background="@drawable/info_card_bg"
            android:textColor="@color/primary"
            android:textSize="12sp"
            android:text="@string/info_hash_compute"
            android:visibility="gone" />
    </LinearLayout>
</LinearLayout>
