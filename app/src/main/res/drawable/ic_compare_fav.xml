<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="@color/cmp_only">
    <!--
      Dedicated to the "Compare favorites" root group: a folder (container, with the same semantics as the other root nodes in the tree) with two panels punched out side by side (the "two sides" of the compare). The favorites row itself uses the same ic_compare (two columns + arrow) as the action bar — the two silhouettes are completely different (solid folder vs. thin two-column), not just distinguished by color.
      The cutout uses evenOdd: the row background itself shows through, so selection and light/dark themes do not break the illusion.
    -->
    <path
        android:fillColor="@android:color/white"
        android:fillType="evenOdd"
        android:pathData="M10,4H4c-1.1,0 -1.99,0.9 -1.99,2L2,18c0,1.1 0.9,2 2,2h16c1.1,0 2,-0.9 2,-2V8c0,-1.1 -0.9,-2 -2,-2h-8l-2,-2zM6.8,10.5h3.1v6.2H6.8zM14.1,10.5h3.1v6.2h-3.1z" />
</vector>
