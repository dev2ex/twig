# Android / Gradle
*.iml
.gradle/
/local.properties
.idea/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab

# Signing key must never enter the repo
*.keystore
*.jks
keystore.properties

# Machine-local: test device address, release directory, local service credentials
CLAUDE.local.md
.claude/

# Internal security reviews / audit reports — findings, file:line and repro details
# for issues in this codebase. Never publish these alongside the source.
docs/REVIEW_*.md
