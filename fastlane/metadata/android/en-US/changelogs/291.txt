Internal only: the build now uses a newer R8 than the one bundled with the Android Gradle Plugin. F-Droid rebuilds each release from source and checks the result byte for byte against the published APK; the previous release matched in everything except one string R8 writes into the compiled code, which turned out to vary between machines. Pinning a newer R8 picks up the determinism fixes made upstream. No user-visible change.
