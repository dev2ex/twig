Files can now get their own home-screen shortcut, not just directories: pick how it opens — automatically, as text, as hex, or with one specific app chosen up front — and the icon uses the file's own thumbnail when one's already cached. Local files open with no master-password prompt; server files reconnect first. Shortcuts now run in their own task, so opening one no longer flashes the main screen or returns to it on back.
